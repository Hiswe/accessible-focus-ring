extends _layout.pug

block vars
  - var pageTitle = `global`

block nav
  +menu(0)

block content
  p not done yet
  script( src="https://unpkg.com/@hiswe/focus-ring/dist/focus-ring.min.js")
  script.
    (function () {
      focusRing({
        onTab: function onTab() {
          console.log('tabbed!')
          const domCheckWrapper = document.querySelector('.js-tab')
          domCheckWrapper.innerHTML = 'tabbed: re-enable focus ring'
        }
      });
    }());
